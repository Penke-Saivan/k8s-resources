apiVersion: v1
kind: Pod
metadata:
  name: multi-containers
  # namespace: roboshop
  labels:
    environment: development
    project: roboshop
spec: 
  containers:
  #list of contAiners
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
  - name: almalinux
    image: almalinux:9
    command: ["sleep", "1000"] #almalinx does not run as nginx runs