apiVersion: v1
kind: Pod
metadata:
  name: frontend
  labels:
    env: dev
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "64Mi"
        cpu: "100m" #1/10th of cpu processor
      limits:
        memory: "100Mi"
        cpu: "120m"        